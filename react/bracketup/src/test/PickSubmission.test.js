import React from "react";
import { render, fireEvent, waitFor } from "@testing-library/react";
import { AllPicks } from "../components/AllPicks";

jest.mock("axios");

test("correct pick submission");
